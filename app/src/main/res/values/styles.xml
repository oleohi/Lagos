<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style for a tab that displays an Attraction name -->
    <style name="AttractionTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">#4CAF50</item>
        <item name="tabSelectedTextColor">#AED581</item>
        <item name="tabTextAppearance">@style/AttractionTabTextAppearance</item>
    </style>

    <!-- Text appearance style for an Attraction tab -->
    <style name="AttractionTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
